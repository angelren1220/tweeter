{
  "name": "tweeter",
  "version": "1.0.0",
  "description": "A simple single page AJAX-based Tweeter clone that uses jQuery, HTML5 and plain ol' CSS3.",
  "main": "server/index.js",
  "scripts": {
    "sass-watch": "sass sass:public/styles --watch --poll --no-source-map",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server/index.js",
    "local": "./node_modules/.bin/nodemon --watch server -e js server/index.js"
  },
  "author": "Angel Linxue Ren",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.15.2",
    "chance": "^1.0.2",
    "express": "^4.13.4",
    "md5": "^2.1.0",
    "sass": "^1.60.0"
  },
  "devDependencies": {
    "nodemon": "^1.9.2"
  }
}
